# Preset "example" values intentionally look like real API keys so the
# example-matches-pattern tests stay meaningful. Exclude them from secret
# scanning push protection.
paths-ignore:
  - "presets.ts"
  - "tests/**"
