# pi-data-masking 配置体验改造需求方案

> 状态：P0/P1 已实施，P2 交互增强已实施
> 版本：1.0
> 目标版本：待排期
> 适用范围：配置模型、配置加载、Pi 命令与 TUI、文档和迁移兼容

实施进度（2026-08-23）：阶段一和阶段二均已实现；阶段三已补齐新增、编辑、删除、同作用域排序、筛选、搜索、批量启停、测试及安全导入/脱敏导出，并增加面向用户的 `name` 与自动唯一 `id`。根据实际交互反馈，单条规则改为空格后立即原子生效，不弹确认框，仅在关闭后显示非阻塞风险通知；不再要求 `Ctrl+S`。无交互写命令和 JSONC 仍按真实需求评估，不默认加入。

## 1. 需求摘要

保留 JSON 作为稳定、可审查的底层持久化格式，在此基础上提供更直观的配置入口：

1. 提供 `/masking-config` 统一配置入口，覆盖查看、初始化、启停、编辑和测试；
2. 支持对每一条已有规则单独启用或关闭；
3. 提供常用敏感数据的内置规则预设，普通用户不再需要复制和维护长正则；
4. 提供 JSON Schema、最小配置和初始化向导；
5. 支持从环境变量读取 literal 规则的真实值，减少明文秘密落盘；
6. 保留现有 JSON 配置、全局/项目合并顺序和热加载行为。

配置文件继续作为事实来源（source of truth），TUI 负责安全地读取、编辑、校验和原子写回配置，而不是维护另一套独立状态。

## 2. 背景与问题

当前方案具备无额外解析依赖、配置可审查、支持热加载等优点，但用户配置成本较高：

- JSON 不支持注释，示例文件使用 `_comment`、`_usage` 等字段承载说明；
- 正则中的反斜杠需要双重转义，长规则不易阅读和检查；
- 初次使用需要复制完整示例，再删除大量不需要的规则；
- 用户需要理解 literal/regex、捕获组、规则顺序、全局/项目合并等概念；
- 现有 `/masking-list` 只能查看，不能逐条启停、编辑或调整优先级；
- literal 规则的 `real` 可能把真实秘密以明文写入配置文件；
- 扩展只有总开关，临时停用某条高误报规则时必须删除规则或修改其结构，不利于保留配置和排障。

## 3. 目标与非目标

### 3.1 目标

- 新用户无需编写正则即可完成常见配置；
- 用户可以在不删除规则的情况下逐条启用或关闭；
- 高级用户仍可直接编辑完整 JSON 和自定义正则；
- 所有修改均可预览、校验、撤销或通过版本控制审查；
- 旧配置升级后行为保持不变；
- 配置写入过程不泄露真实值，并尽量避免秘密进入仓库。

### 3.2 非目标

- 不把本扩展改造成通用 PII 自动识别系统；
- 不支持执行 JavaScript/TypeScript 配置文件；
- 不在本期引入远程配置中心或跨设备同步；
- 不改变“项目规则优先于全局规则”的既有合并语义；
- 不保证启用规则后能撤回模型在此前请求中已经看到的内容。

## 4. 核心概念和状态模型

系统存在两级启停状态：

1. **扩展总开关**：顶层 `enabled` 以及 `/masking-toggle` 持久化覆盖值；
2. **单条规则开关**：规则字段 `enabled`。

一条规则的最终生效条件为：

```text
扩展最终 enabled = true
AND
规则 enabled != false
```

其中规则未声明 `enabled` 时按 `true` 处理，以保证所有历史配置兼容。

### 4.1 单条规则配置示例

```json
{
  "id": "github_pat",
  "type": "regex",
  "enabled": false,
  "description": "GitHub personal access token",
  "pattern": "\\bghp_[A-Za-z0-9]{36}\\b"
}
```

关闭后的规则必须：

- 保留在原配置文件和原顺序中；
- 不参与匹配、占位符生成、冲突检测和运行时规则计数；
- 仍出现在配置界面和规则列表中，并明确标记为“已关闭”；
- 仍接受结构校验，使用户重新启用前能够发现错误；
- 不因关闭而丢失 `description`、`placeholder`、`preserveStructure` 等配置。

### 4.2 作用域

继续支持：

- 全局配置：`~/.pi/agent/pi-data-masking/masking.config.json`；
- 项目配置：`<project>/.pi/pi-data-masking/masking.config.json`。

项目规则始终排在全局规则之前。单条规则开关只修改该规则所属的源文件：

- 关闭项目规则不会自动关闭同 ID 的全局规则；
- 关闭全局规则不会修改项目配置；
- TUI 必须显示每条规则的作用域和配置路径，避免同 ID 规则产生歧义；
- 调整顺序只能在同一作用域内进行，不能越过项目/全局边界。

`id` 是用于安全写回定位、并发变更检查、告警和排障的稳定机器标识，
而非展示名称。同一配置文件内必须唯一；project 与 global 可以各自存在
一个相同 `id`，两者通过作用域、路径和源位置区分。`name` 是配置中心主要
展示的简短规则名称，`description` 用于可选的较长说明。TUI 新建规则时
要求输入 `name`，并自动派生可读且在目标文件内不重复的 `id`；内置预设规则
先展示用途说明并选择预设，直接采用预设的显示名称，同时将完整正则模板写入
配置，便于继续修改，无需提前输入名称；旧的 `preset` 引用在编辑时展开；旧配置无需
迁移，展示时按 `name ?? description ?? id` 回退。

精确 literal 在配置首页保持隐藏，主动进入编辑器后默认显示真实值，并通过
`Ctrl+R` 原地隐藏/恢复；配置首页通过 `H` 展示总体说明，规则编辑器通过 `F1`
展示上下文字段、正则格式和示例。切换或删除规则后应保留相邻位置，删除末条时
选择新的末条。

## 5. 用户故事

### US-01：首次初始化

作为首次使用者，我希望进入 `/masking-config` 后通过二级菜单选择作用域和常用预设，不必记忆额外的初始化命令，也不必复制并删改一份大型示例文件。

### US-02：逐条关闭误报规则

作为使用者，我希望在规则列表中关闭某一条误报规则，同时保留其内容和顺序，方便后续重新启用。

### US-03：逐条重新启用

作为使用者，我希望看到所有已关闭规则，并在校验通过后重新启用它们。

### US-04：高级规则编辑

作为高级用户，我希望继续直接编辑 literal 或 regex 规则，并在保存前测试正例和反例。

### US-05：避免秘密落盘

作为安全敏感用户，我希望规则可以从指定环境变量读取真实值，配置文件只保存环境变量名。

### US-06：审查最终生效状态

作为排障人员，我希望区分“已配置规则数”和“当前生效规则数”，并知道规则来自全局还是项目配置。

## 6. 功能需求

### 6.1 命令信息架构

命令应按用户任务而不是底层功能拆分，最终推荐只保留四个主要入口：

| 命令 | 定位 | 保留原因 |
|---|---|---|
| `/masking-config` | 配置中心 | 统一完成查看、创建、逐条启停、编辑、排序、预设选择和本地测试 |
| `/masking-toggle` | 紧急总开关 | 属于对话中高频、一步完成的安全操作，不应要求进入多层菜单 |
| `/masking-test <text>` | 快速验证 | 适合编辑文件后立即测试，也可被脚本或命令历史重复使用 |
| `/masking-history` | 会话审计 | 面向已经发生的会话记录，与配置管理是不同工作流 |

以下命令不作为长期独立入口：

- `/masking-list`：其完整功能由 `/masking-config` 默认首页覆盖；
- `/masking-init`：初始化放入 `/masking-config` 的“配置来源/创建配置”二级菜单；
- `/masking-rule`：没有明确的无交互自动化需求前不新增，避免重新制造命令碎片。

不建议把所有功能进一步合并成单个 `/masking` 命令加子参数。这样虽然命令数量更少，但会降低命令补全的可发现性，并让高频的 toggle/test 操作增加输入成本。

### 6.2 `/masking-list` 兼容策略

`/masking-list` 已经是公开命令，不应立即静默删除：

1. 第一阶段将其改为打开 `/masking-config` 的默认规则列表页，并显示一次迁移提示；
2. README 和命令说明只推荐 `/masking-config`；
3. 后续大版本可删除该别名；若保留别名几乎没有维护成本，也可长期兼容，但不再扩展其独立功能。

`/masking-init` 尚未发布，因此直接取消独立命令设计，不需要兼容期。

### FR-01：规则级 `enabled` 字段（P0）

- literal、regex 和预设引用规则均支持 `enabled?: boolean`；
- 缺省值为 `true`；
- `enabled: false` 表示保留但不运行；
- 若 `enabled` 是非布尔值，加载器必须产生明确警告，并以安全方式不激活该规则；
- 顶层总开关关闭时，不改变各规则原有的 `enabled` 值；
- 重新打开总开关时，只恢复单条规则开关为开启的规则。

### FR-02：加载器保留全部配置规则（P0）

加载后的内部模型需要区分：

- `configuredRules`：包括启用和关闭的全部合法规则，供列表、编辑和写回使用；
- `activeRules`：仅包含最终启用的规则，传给 `Masker`。

不得通过在加载阶段直接删除关闭规则来实现停用，否则 TUI 无法展示和恢复它们。

关闭规则的正则编译错误或字段错误仍应报告，但警告要带有“该规则当前已关闭”的上下文，不应影响其他规则加载。

### FR-03：配置首页的规则状态展示（P0）

`/masking-config` 默认首页即为合并后的规则列表，每条规则至少展示：

- 开关状态：`ON` / `OFF`；
- ID；
- 类型：literal、regex 或 preset；
- 作用域：project/global；
- 描述；
- regex pattern 或 literal placeholder，继续隐藏 literal 的真实值；
- 当前优先级。

状态栏由原来的规则总数改为类似：

```text
🔒 Masking: 8 active / 12 configured
```

总开关关闭时显示：

```text
🔓 Masking: off · 8 rules ready
```

### FR-04：`/masking-config` 配置界面（P0）

新增全屏 TUI，默认同时读取全局和项目配置。首页同时承担原 `/masking-list` 的浏览能力，不要求用户先进入“编辑模式”。建议交互：

| 操作 | 行为 |
|---|---|
| `↑` / `↓` | 选择规则 |
| `Space` | 立即切换所选规则启用/关闭；关闭后显示非阻塞风险通知 |
| `Enter` | 查看或编辑规则详情 |
| `A` | 添加规则或预设 |
| `M` | 打开“配置来源”二级菜单 |
| `Tab` | 在规则列表和首页内嵌测试区之间切换焦点 |
| `T` | 快速聚焦首页内嵌测试区，不再打开独立输入框 |
| `H` | 显示规则类型、字段、正则语法和示例帮助 |
| `Ctrl+↑` / `Ctrl+↓` | 在同一作用域内调整优先级 |
| `Esc` | 退出配置中心或取消当前编辑器 |

界面要求：

- 明确显示当前编辑的是全局配置还是项目配置；
- 首页浏览规则不产生任何配置修改；
- 首页和规则详情隐藏 literal 真实值；用户主动进入规则编辑器后默认显示，
  `Ctrl+R` 可在不离开编辑器的情况下临时隐藏或恢复；
- 单条开关立即原子写入并热加载，不维护额外启停草稿或阻塞确认；
- 保存失败不得覆盖原文件；规则编辑器应保留编辑内容供用户修正；
- 禁止跨作用域拖动或重排规则；
- 支持筛选“全部、启用、关闭、项目、全局、literal、regex、preset”。

首页底部提供常驻、紧凑的“Test active rules”区域。默认焦点仍在规则列表，
测试区在小终端中可折叠；`Tab` 切换焦点，`T` 直接聚焦。测试文本变化后在
本地短暂防抖并自动生成 masked preview、匹配次数和规则归属；关闭配置中心后
清空，不写入配置、会话历史、动态映射或模型上下文。总开关关闭时仍可预览
所有逐条启用且当前可用的规则，但必须明确标为仅预览。

首页的两个区域必须对称表达焦点：规则区和测试区标题均提供 `▶`、accent/bold
或边框状态，不能只高亮输入框。标题和说明不得被额外分割线包围；Rules 标题之后才
显示上分割线，下分割线放在规则列表和所选规则详情之后，只包围具体规则内容。测试
输入区直接使用编辑器自身的上下边框，不得再叠加一组外层分割线。测试区未聚焦且为空时直接显示
“Press Tab or T to focus and paste sample text”，聚焦后显示“Tab returns to rules”；
底部快捷键只作为补充，不能作为唯一的操作发现入口。

规则编辑器采用上下双区域：上方编辑规则 JSON，下方输入测试文本并显示结果。标题和
说明位于编辑器边框之前；JSON 和测试输入直接使用编辑器自身边框，不增加外层分割线。
`Tab`/`Shift+Tab` 切换焦点，默认仅用当前草稿规则测试。底部常驻简短快捷键，
`F1` 原地展开/收起当前规则类型的字段和示例说明（字母键保留给输入），不弹出
会丢失草稿的独立页面。
已有规则继续使用 JSON 与测试双区域编辑器。

新增规则保留两个简短的前置选择：配置来源和规则大类；内置预设类型额外先进入专用
预设列表，完整列出每个预设的名称、稳定标识和脱敏格式说明，选择后才进入 Rule
Builder。Builder 替代“名称 → 类型字段 → Review”的后续多步骤向导，只显示该
规则类型的上下文字段、测试输入和实时预览，不再重复显示可编辑的 scope/type；
结构化表单上方使用紧凑字段列表，每行只显示字段名和当前值；下方保留固定高度的编辑
区，集中显示当前字段名、说明和使用几乎完整终端宽度的输入内容。切换字段时编辑区位置
和总高度不得改变，列表中的其他已输入值仍保持可见；长内容在编辑区围绕光标水平滚动，
列表摘要按可用宽度截断。`Up`/`Down` 遍历结构化字段，
`Tab`/`Shift+Tab` 只在表单与测试区之间切换，`Left`/`Right` 或 `Space` 修改选择型
字段。非预设规则的名称、说明、正则、flags、环境变量和自定义 placeholder 等业务
字段默认留空，示例只能出现在说明或帮助中。`F1` 展开字段帮助，`F2` 在结构化表单与
完整 JSON 之间切换，`Ctrl+S` 校验并保存，`Esc` 取消。`name` 改变时自动生成在目标
文件内唯一的 `id`，除非用户在 JSON 模式显式修改。选中的内置预设进入 Builder 时
立即展开为可继续修改的名称、说明、pattern、flags 和结构保持配置。新增规则的结构化
字段没有编辑器边框，因此只在具体字段列表上下显示两条分割线；JSON 和测试输入已有
编辑器边框，不得重复增加分割线。保存前已有实时校验和预览，因此不再增加独立 Review 页。

“配置来源”二级菜单至少提供：

- 查看全局和项目配置路径及存在状态；
- 创建缺失的全局配置；
- 创建缺失的项目配置；
- 从最小模板或预设集合初始化；
- 打开已有配置进行编辑；
- 对可能被 Git 跟踪的项目配置给出安全提示。

当两个配置文件都不存在时，首页显示空状态，并直接提供“创建项目配置”和“创建全局配置”两个入口，不要求用户先打开二级菜单。

### FR-05：启停安全提示（P0）

规则启停会影响发送给模型的上下文，必须明确提示：

- **关闭规则**：提示该规则匹配的值可能在后续请求中以明文发送给模型；
- **启用规则**：提示只对后续模型边界生效，无法撤回此前已经发送的内容；
- 当前会话已有消息时，启用或关闭规则后建议新建会话，以获得最清晰的一致性边界；
- TUI 保存后配置热加载，用户无需重启 Pi。

单条关闭不使用阻塞确认，以保证开关操作足够快捷；写入成功后使用 warning
通知风险。批量关闭和删除规则影响范围更大，仍需一次汇总确认。

### FR-06：`/masking-config` 内置初始化流程（P1）

在 `/masking-config` 的“配置来源”二级菜单中提供初始化流程：

1. 选择 global 或 project；
2. 选择常用预设；
3. 配置是否显示状态栏、是否持久化历史等选项；
4. 预览将要创建的配置；
5. 校验并以原子方式写入。

如果目标文件已经存在，不得直接覆盖，应返回对应配置的编辑页面或要求用户明确确认合并。

### FR-07：内置预设（P1）

将当前示例中的通用正则提取为代码内置、经过测试的命名预设，例如：

- `github-pat`；
- `npm-token`；
- `huggingface-token`；
- `aws-access-key-id`；
- `slack-token`；
- `jwt`；
- `pem-private-key`；
- `bearer-token`；
- `database-userinfo`；
- `private-ipv4`。

建议引用格式：

```json
{
  "id": "github_pat",
  "preset": "github-pat",
  "enabled": true
}
```

要求：

- 预设在加载时展开为普通运行时规则；
- 引用位置决定优先级；
- 用户可以逐条关闭预设引用；
- 未知预设必须警告并保持不激活；
- 预设定义的行为变化必须记录在 CHANGELOG 中；
- 自定义 regex 始终作为高级逃生口保留。

### FR-08：环境变量值来源（P1）

literal 规则增加 `realFromEnv`，与 `real` 二选一：

```json
{
  "id": "prod_api_key",
  "enabled": true,
  "realFromEnv": "PROD_API_KEY",
  "preserveStructure": { "keepPrefix": true }
}
```

要求：

- 环境变量缺失或为空时，该规则不得激活，并显示警告；
- 不得把解析出的真实值写回配置、日志、通知或 TUI；
- `/masking-config` 只显示环境变量名，不显示变量值；兼容期内的 `/masking-list` 别名遵循相同要求；
- `real` 与 `realFromEnv` 同时出现时视为配置错误，不得猜测优先级；
- 环境变量变化在配置热加载或新会话开始时重新读取。

### FR-09：JSON Schema（P1）

提供 `masking.config.schema.json`，覆盖：

- 顶层字段和默认值；
- literal、regex、preset 三种互斥规则结构；
- `enabled`、`lowEntropy`、`preserveStructure` 等字段；
- `real` / `realFromEnv` 二选一；
- regex 不允许固定 placeholder；
- 字段说明和示例。

配置允许使用 `$schema` 字段，使 VS Code 等编辑器提供自动补全和即时错误提示。

### FR-10：最小示例与高级示例分离（P1）

- `masking.config.example.json` 改为最小可运行示例；
- 通用检测规则通过预设选择，不再把所有长正则复制到用户配置；
- 高级正则、捕获组、lookahead 和优先级案例移动到 README 或独立高级示例；
- 配置文件中不再使用大量 `_comment` 伪字段。

### FR-11：无交互操作评估（P2，可选）

默认不新增逐条规则命令。只有收集到明确的脚本化、CI 或无 TUI 环境需求后，才评估提供例如以下接口：

```text
/masking-rule project github_pat on
/masking-rule global private_ip_address off
```

要求：

- 必须显式指定作用域，避免同 ID 歧义；
- 找不到规则时不修改任何文件；
- 操作前后都不显示 literal 真实值；
- 写入和安全确认要求与 TUI 相同。

若无法在无交互环境可靠表达关闭规则的风险确认，则只提供读取/校验能力，不提供写操作。

### FR-12：JSONC（P2，可选）

在 TUI、预设和 Schema 上线后，根据真实反馈决定是否增加 `.jsonc` 支持。JSONC 只解决注释和尾逗号问题，不作为配置体验改造的前置条件。

若支持 JSONC：

- 原 `.json` 路径和严格 JSON 行为必须保留；
- 同一作用域同时存在 `.json` 和 `.jsonc` 时必须报错，不得静默选择；
- 写回时尽量保留用户注释，否则只提供只读加载能力。

## 7. 配置示例

目标形态示例：

```json
{
  "$schema": "./masking.config.schema.json",
  "version": 1,
  "enabled": true,
  "rules": [
    {
      "id": "company_root_domain",
      "enabled": true,
      "real": "company-internal.com",
      "placeholder": "northstar-systems.com"
    },
    {
      "id": "prod_api_key",
      "enabled": true,
      "realFromEnv": "PROD_API_KEY",
      "preserveStructure": { "keepPrefix": true }
    },
    {
      "id": "private_ip_address",
      "preset": "private-ipv4",
      "enabled": false,
      "preserveStructure": { "keepIPv4Octets": 2 }
    },
    {
      "id": "custom_employee_email",
      "type": "regex",
      "enabled": true,
      "pattern": "[A-Za-z0-9._%+-]+(?=@company-internal\\.com)"
    }
  ],
  "options": {
    "caseSensitive": true,
    "showStatusBar": true,
    "systemPromptGuidance": false,
    "persistHistory": true
  }
}
```

## 8. 校验与错误处理

### 8.1 保存前校验

TUI 和初始化向导保存前必须执行与运行时加载器相同的校验：

- ID 非空；
- 同一文件内 ID 唯一；
- `enabled` 类型正确；
- literal 值来源有效；
- regex 可以编译；
- preset 存在；
- placeholder 合法；
- `preserveStructure` 参数在合法范围；
- 低熵规则显示风险警告。

错误阻止保存；风险警告允许用户确认后保存。

### 8.2 运行时加载

- 单条错误规则不应阻止其他合法规则加载；
- 解析整个文件失败时保留上一份成功加载的运行时配置，不应退回空规则后继续运行；
- 通知必须包含作用域、规则 ID 和错误原因；
- 通知、日志和错误堆栈不得包含 literal 真实值或环境变量解析值。

## 9. 安全要求

1. 新建或重写包含 literal 值的配置文件时，权限必须为 `0600`；
2. 使用临时文件加原子 rename，失败时保留原配置；
3. TUI 中秘密输入框默认掩码显示；
4. 不把真实值写入命令历史、通知、状态栏或调试日志；
5. 创建项目配置时提示其可能被 Git 跟踪，并提供加入 `.gitignore` 的明确选项；
6. 环境变量引用优先推荐给 API key、token、密码等高敏感 literal 值；
7. 导出、复制或展示配置时默认脱敏 `real`；
8. 关闭规则前明确提示未来上下文可能泄露匹配值。

## 10. 兼容性和迁移

- 没有 `version`、`$schema`、`enabled` 的旧配置继续可用；
- 旧规则的单条 `enabled` 默认视为 `true`；
- 现有 literal 和 regex 字段语义不变；
- 现有全局/项目合并顺序不变；
- 现有 `/masking-toggle` 的持久化覆盖行为不变；
- 首次升级不自动改写用户配置；
- 只有用户通过 TUI 保存时，才补充新字段或规范化格式；
- 配置热加载继续复用当前会话的 session key 和动态映射。

## 11. 验收标准

### AC-01：旧配置兼容

给定一份不含规则级 `enabled` 的现有配置，升级后所有规则的匹配结果、顺序和占位符行为与升级前一致。

### AC-02：关闭 literal 规则

给定一条 `enabled: false` 的 literal 规则，匹配文本不被替换；规则仍显示在列表中，且真实值不被展示。

### AC-03：关闭 regex 规则

给定一条 `enabled: false` 的 regex 规则，该规则不创建动态占位符映射，也不影响后续低优先级规则匹配。

### AC-04：总开关与单条开关联动

给定三条规则，其中两条开启、一条关闭：

- 总开关开启时只运行两条；
- 总开关关闭时一条都不运行；
- 再次打开总开关时仍只运行原来的两条。

### AC-05：TUI 即时启停

在 `/masking-config` 中按空格关闭一条规则后：

- 对应源文件只改变该规则的 `enabled`；
- 配置能够热加载；
- 状态栏 active 数量减少；
- 重启 Pi 后状态保持。

### AC-06：取消编辑

在 TUI 修改规则后按 Esc 并确认放弃，磁盘文件和运行时配置均不变化。

### AC-07：作用域隔离

项目和全局配置存在相同 ID 时，关闭项目规则只修改项目文件，全局规则保持原状态并继续按既有优先级参与匹配。

### AC-08：环境变量引用

环境变量存在时规则正常生效；缺失时规则不激活并发出不含真实值的警告。

### AC-09：写入安全

配置保存中断或校验失败时原文件保持完整；成功创建的新文件权限为 `0600`。

### AC-10：启停提示

在已有会话中保存规则启停变化时，用户能看到“无法撤回此前已发送内容，建议新会话”的提示。

## 12. 测试范围

### 单元测试

- `enabled` 缺省、true、false、非法类型；
- configuredRules/activeRules 分离；
- 关闭规则不参与匹配和占位符冲突；
- 总开关与单条开关组合；
- global/project 同 ID 隔离；
- preset 展开和未知 preset；
- `realFromEnv` 存在、缺失、空值、与 `real` 冲突；
- Schema 正反例；
- 原子写入和权限。

### 集成测试

- TUI 启停、保存、放弃和热加载；
- 规则顺序调整；
- 配置解析失败后保留上一份可用配置；
- Pi 重启后单条规则状态保持；
- 通知和日志不泄露真实值。

### 回归测试

- 现有 literal、regex、capture group、lookahead、preserveStructure；
- placeholder 稳定性；
- history persistence；
- `/masking-test`、`/masking-list`、`/masking-toggle`。

## 13. 实施阶段建议

### 阶段一：规则级启停（P0）

- 配置模型增加 `enabled`；
- loader 保留 configuredRules 并生成 activeRules；
- `/masking-config` 默认首页展示全部规则、状态和作用域；
- `/masking-config` 支持逐条启停、安全确认和保存；
- `/masking-list` 临时作为打开配置首页的兼容别名；
- 补齐单元、集成和兼容性测试。

### 阶段二：降低配置门槛（P1）

- `/masking-config` 配置来源二级菜单和内置初始化流程；
- 内置预设；
- `realFromEnv`；
- JSON Schema；
- 最小示例和文档重构。

### 阶段三：增强能力（P2）

- 根据真实自动化需求评估无交互接口；
- 根据反馈评估 JSONC；
- TUI 批量操作、搜索和导入导出。

## 14. 产品决策记录

1. 配置中心包含新增、编辑、删除和同作用域排序；
2. 单条启停均直接写入，关闭后显示非阻塞风险通知；批量关闭仍在一次汇总确认后写入；
3. 项目初始化仅提示 Git 风险，并由用户选择是否写入 `.gitignore`；
4. 预设不允许覆盖 pattern/flags，仅开放 `name`、`description`、`enabled`、`lowEntropy` 和 `preserveStructure`；
5. 尚无明确的 CI 或无 TUI 写操作需求，因此不新增 `/masking-rule`；
6. 尚无必须保留 JSON 注释的反馈，因此不引入可能破坏注释写回的 JSONC。
7. 新建 exact literal 不重复确认存储意图；输入真实值后选择自动 placeholder
   或精确自定义替换，最终摘要只显示模式而不回显具体值。
